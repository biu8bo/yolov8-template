# YOLOv8 训练教程


```
建议先去找PyTorch + CUDA安装教程，使用GPU训练
```
---

## 一、数据集准备

### 1.1 目录结构

```
dataset/
├── data.yaml              # 数据集配置文件（核心）
├── train/
│   ├── images/            # 训练集图片
│   │   ├── img_001.png
│   │   └── ...
│   └── labels/            # 训练集标签（YOLO txt 格式）
│       ├── img_001.txt
│       └── ...
├── valid/                 # 验证集（也可命名为 val）
│   ├── images/
│   └── labels/
└── test/                  # 测试集（可选，不参与训练和验证）
    ├── images/
    └── labels/
```

---

### 1.2 图片和标签对应关系

**一张图必须对应一个同名 `.txt` 标签文件**，放在 `labels/` 下：

```
train/images/img_001.png    →    train/labels/img_001.txt
train/images/img_002.png    →    train/labels/img_002.txt
```

---

### 1.3 YOLO txt 标签格式

每行一个目标，格式为：

```
<class_id> <x_center> <y_center> <width> <height>
```

**关键规则：**

| 规则 | 说明 |
|------|------|
| 数值类型 | 全部是**归一化到 [0, 1] 的相对值**，不是像素坐标 |
| x_center | 目标中心 x / 图片宽度 |
| y_center | 目标中心 y / 图片高度 |
| width | 目标框宽度 / 图片宽度 |
| height | 目标框高度 / 图片高度 |
| class_id | 从 0 开始，如 0=cone（路锥） |

**示例：**

一张 1920×1080 的图中有一个路锥，框为 `(x, y, w, h) = (800, 500, 120, 200)`：

```
0 0.416 0.463 0.063 0.185
```

计算过程：

```
x_center = 800 / 1920  = 0.416
y_center = 500 / 1080  = 0.463
width    = 120 / 1920  = 0.063
height   = 200 / 1080  = 0.185
```

---

## 二、data.yaml 详解

`data.yaml` 是训练的核心配置文件，告诉 YOLOv8 去哪找数据、有哪些类别。

### 2.1 当前项目的 data.yaml

```yaml
# 数据集划分路径（相对于 data.yaml 所在目录）
train: ./train/images      # 训练集图片目录
val: ./valid/images        # 验证集图片目录（标签自动去 labels/ 找）
test: ./test/images        # 测试集图片目录（可选）

# 类别定义
nc: 1                      # 类别数量
names: ['cone']            # 类别名称列表，索引从 0 开始

```

### 2.2 参数详解

| 参数 | 必须 | 说明 |
|------|:--:|------|
| `train` | ✅ | 训练集图片目录路径。标签文件自动从 `../labels/` 对应位置读取 |
| `val` | ✅ | 验证集图片目录路径。每轮训练结束后在此集上评估 mAP |
| `test` | ❌ | 测试集图片目录路径。不参与训练，仅用于最终评估 |
| `nc` | ✅ | 类别数量（number of classes） |
| `names` | ✅ | 类别名称列表，`names[0]` = 第 0 类名称 |

### 2.3 标准格式 vs Roboflow 格式

两种写法效果一样，关键区别在于 `path` 字段：

**标准 YOLOv8 格式（推荐自行标注时使用）：**

```yaml
path: ./datasets        # 数据集根目录
train: train/images    # 相对于 path 的路径
val: val/images        # 相对于 path 的路径

nc: 3
names: ['dog', 'cat', 'person']
```

**（当前项目使用）：**

```yaml
# 没有 path 字段，直接写相对于 data.yaml 的相对路径
train: ./train/images
val: ./valid/images
test: ./test/images

nc: 1
names: ['cone']
```

### 2.4 data.yaml 路径解析规则

```
dataset/                    ← data.yaml 在这里
├── data.yaml
├── train/
│   ├── images/001.png      ← train 指向这里
│   └── labels/001.txt      ← YOLOv8 自动找到这里
├── valid/
│   ├── images/002.png      ← val 指向这里
│   └── labels/002.txt      ← YOLOv8 自动找到这里
└── test/
    ├── images/003.png      ← test 指向这里
    └── labels/003.txt      ← YOLOv8 自动找到这里
```

**YOLOv8 自动推断标签路径的规则：**
- `images/` → 自动替换为 `labels/`
- 所以只需要指定图片目录，标签目录不用写

---

## 三、开始训练

### 3.1 环境准备

```bash
# 安装依赖
pip install ultralytics torch opencv-python

# 或直接用项目 requirements.txt
cd D:\Dev\yolov8
pip install -r requirements.txt
```

### 3.2 快速开始

```bash
# 默认配置训练（yolov8n，100 轮，imgsz=640）
python train.py

# 指定大模型、更多轮数
python train.py --model yolov8s.pt --epochs 200 --batch 16

# 小物体多需要大尺寸输入
python train.py --imgsz 1280 --batch 4
```

### 3.3 常用训练策略

| 场景 | 命令 | 说明 |
|------|------|------|
| 初次实验 | `python train.py` | 默认 yolov8n，快速看效果 |
| 追求精度 | `python train.py --model yolov8s.pt --epochs 200` | 更大的模型 + 更多轮次 |
| 小目标多 | `python train.py --imgsz 1280 --batch 4` | 大尺寸输入，batch 需相应减小 |
| 数据增强 | `python train.py --augment` | 开启更激进的 mosaic + mixup |
| 继续训练 | `python train.py --resume` | 从上次中断处恢复 |
| 锁定实验名 | `python train.py --name cone_v2` | 方便区分多次实验 |

---

## 四、训练结果

### 4.1 输出目录

```
runs/train/exp/
├── weights/
│   ├── best.pt         ← ★ 验证集 mAP 最高的模型
│   └── last.pt         ← 最后一轮的模型（用于 resume）
├── results.csv         # 每轮的 loss / mAP / precision / recall
├── results.png         # 训练曲线图
├── confusion_matrix.png
├── val_batch0_pred.jpg # 验证集预测可视化
└── args.yaml           # 本次训练的参数快照
```

### 4.2 核心指标解读

| 指标 | 含义 | 好 |
|------|------|----|
| **mAP50** | IoU=0.5 时的平均精度 | ≥ 0.85 |
| **mAP50-95** | IoU 从 0.5 到 0.95 的平均精度 | ≥ 0.5 |
| **Precision** | 预测为正的样本中真正为正的比例 | ≥ 0.8 |
| **Recall** | 所有正样本中被正确检出的比例 | ≥ 0.8 |
| **box_loss** | 定位损失（越小越好） | 持续下降 |
| **cls_loss** | 分类损失 | 持续下降 |

---

## 五、推理验证

```bash
# 用最佳模型推理单张图
python predict.py --model runs/train/exp/weights/best.pt --source test.png

# 批量推理测试集
python predict.py --model runs/train/exp/weights/best.pt --source datasets/test/images/

# 调高阈值减少误检
python predict.py --model runs/train/exp/weights/best.pt --source test.png --conf 0.5
```

---

## 六、模型导出

```bash
# 导出 ONNX 格式（默认）
python export.py

# 指定训练好的模型
python export.py --model runs/train/exp/weights/best.pt

# 动态尺寸 + 高 opset
python export.py --dynamic --opset 17
```

---

## 七、关键注意事项

| # | 事项 |
|---|------|
| 1 | 标签格式必须是**归一化相对值**，不是像素坐标 |
| 2 | 一张图对应一个同名 `.txt` 标签，缺标签的训练不会启动 |
| 3 | `imgsz` 必须是 **32 的倍数**，否则自动截断（1080 → 1056） |
| 4 | 训练和推理的 `imgsz` 要**一致** |
| 5 | `best.pt` 是按 mAP 曲线选的最优轮次，不是最后一轮 |
| 6 | 小目标（占图 < 3%）建议 `imgsz ≥ 1024` |
| 7 | 类别 `nc` 和 `names` 数量必须一致 |
